apiVersion: v1
kind: Service
metadata:
  name: openai-oauth-proxy
  labels:
    app.kubernetes.io/name: openai-oauth-proxy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: openai-oauth-proxy
  ports:
    - name: http
      port: 8080
      targetPort: http
