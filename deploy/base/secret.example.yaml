# Example only: this file is deliberately excluded from kustomization.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: openai-oauth-proxy
type: Opaque
stringData:
  # These invalid short markers make the application fail closed if applied unchanged.
  BIFROST_PROXY_TOKEN: NOT_A_SECRET
  METRICS_TOKEN: NOT_A_SECRET
